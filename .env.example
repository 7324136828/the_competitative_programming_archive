# Preferred ports. If either is busy, run.py advances until it finds a free one.
BACKEND_PORT=8000
# Server Configuration
BACKEND_HOST=127.0.0.1
BACKEND_PORT=3001
FRONTEND_PORT=5173
BACKEND_HOST=127.0.0.1
DATABASE_PATH=

# Relative paths are resolved from the project root.
COUNTER_DB_PATH=data/counter.db
# LLM Providers Configuration (the_connector)
# Leave blank to use the built-in deterministic local mock provider
GEMINI_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OPENROUTER_API_KEY=
OLLAMA_HOST=http://localhost:11434
LLM_PROVIDER=
LLM_MODEL=

# Execution and Upload Limits
MAX_UPLOAD_MB=256
AUTO_SEED=1
