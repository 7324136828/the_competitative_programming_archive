## Genesis

I want you to build a Leetcode like application.

Based on the skills, can you create based on the original-project so that it will have everything in the root?

Make sure you follow the guidance of productionization.

Also make sure we can have a UI in reactjs such that the user can use chat functionality.

Create a backend with the python code based on original-project. Make sure you have setup.bat/setup.sh file that will dispatch setup.py such that it will setup virtual environment and run.bat/run.sh such that it will dispatch the frontend and backend? 


## 1

Based on the skill provided "the_connector",
can you fix the following?

You should first fetch the models via the models' api and display which model we are using for the llm tasks below?

Here are the endpoints not working:

http://localhost:5173/api/llm/hint
http://localhost:5173/api/llm/generate-problem

<button class="btn btn-primary" style="font-size: 0.825rem;">Solve This Problem</button>
solve this problem should insert into the database for the problem generated. 

http://localhost:5173/api/llm/generate-testcases


For solved problem:
<td style="padding: 0.85rem 1rem; font-size: 0.85rem; color: rgb(119, 119, 119); font-weight: 600;">1</td>
keep it green and allow 

When submitting C++ code, it sometimes return empty results:

{
    "submissions": [
        {
            "code": "// Solution in C++\n#include <iostream>\n#include <vector>\n#include <string>\n#include <algorithm>\n\nusing namespace std;\n\nint main() {\n    // Fast I/O\n    ios_base::sync_with_stdio(false);\n    cin.tie(NULL);\n\n    long long a, b;\n    if (cin >> a >> b) {\n        cout << (a + b) << \"\\n\";\n    }\n\n    return 0;\n}\n",
            "created_at": "2026-09-20 20:09:00",
            "error": "",
            "id": 3,
            "language": "cpp",
            "output": "",
            "problem_id": 1,
            "runtime_ms": 175.9,
            "status": "Accepted",
            "test_results": [
                {
                    "error": "",
                    "expectedOutput": null,
                    "normalizedOutput": "",
                    "passed": true,
                    "runtimeMs": 175.9,
                    "status": "Success",
                    "stderr": "",
                    "stdout": ""
                }
            ]
        },
        {
            "code": "# Solution in Python 3\nimport sys\n\ndef solve():\n    # Read all input from standard input\n    input_data = sys.stdin.read().split()\n    if not input_data:\n        return\n    \n    # Example for A + B:\n    if len(input_data) >= 2:\n        try:\n            a = int(input_data[0])\n            b = int(input_data[1])\n            print(a + b)\n        except ValueError:\n            print(\"Invalid integer input\")\n\nif __name__ == '__main__':\n    solve()\n",
            "created_at": "2026-09-20 20:07:50",
            "error": "",
            "id": 2,
            "language": "python",
            "output": "",
            "problem_id": 1,
            "runtime_ms": 52.2,
            "status": "Accepted",
            "test_results": [
                {
                    "error": "",
                    "expectedOutput": null,
                    "normalizedOutput": "",
                    "passed": true,
                    "runtimeMs": 52.2,
                    "status": "Success",
                    "stderr": "",
                    "stdout": ""
                }
            ]
        },
        {
            "code": "# Solution in Python 3\nimport sys\n\ndef solve():\n    # Read all input from standard input\n    input_data = sys.stdin.read().split()\n    if not input_data:\n        return\n    \n    # Example for A + B:\n    if len(input_data) >= 2:\n        try:\n            a = int(input_data[0])\n            b = int(input_data[1])\n            print(a + b)\n        except ValueError:\n            print(\"Invalid integer input\")\n\nif __name__ == '__main__':\n    solve()\n",
            "created_at": "2026-09-20 20:03:28",
            "error": "",
            "id": 1,
            "language": "python",
            "output": "",
            "problem_id": 1,
            "runtime_ms": 61.3,
            "status": "Accepted",
            "test_results": [
                {
                    "error": "",
                    "expectedOutput": null,
                    "normalizedOutput": "",
                    "passed": true,
                    "runtimeMs": 61.3,
                    "status": "Success",
                    "stderr": "",
                    "stdout": ""
                }
            ]
        }
    ],
    "success": true
}

Can we implement async status wait from the backend to compile the code and then return?


## 2 

Can you integrate the code with the merging project skill provided 
to merge the functionality of jira into the competitative programming archive 
by making every problem into a story?

And the concept of problem sets, we can attribute to features.

And set of problem sets, we can call epics, which can be created by the user. 

In order to perserve the functionality of submission and testing code and read aloud and hints,

when we open a story and it's associated with a coding problem, 
then we should open up an activity screen which allow the submission and testing etc.

Note: this activity screen might accommedate functionality more than coding. Please take a note of this to make it extensible. 

Change of status for story related to coding will depend on completely by submission status and also the testing results. 

User should be allowed to import stories like what we have done before for coding problem. 

We can also create coding/non-coding story by using ai as well. 
